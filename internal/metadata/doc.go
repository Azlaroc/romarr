// Package metadata is the seam for external metadata providers (game
// discovery, identity, and art). It currently has no active provider; the
// first one lands here when direct provider integration ships.
package metadata
